import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
export const dynamic='force-dynamic';
export default async function Pending(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main className="p-8"><Link href="/login">Return to sign in</Link></main>;const {data:p}=await supabase.from('profiles').select('access_status').eq('id',user.id).single();if(p?.access_status==='active')return <main className="p-8"><Link href="/">Continue to workspace</Link></main>;return <main className="grid min-h-screen place-items-center p-5"><section className="max-w-xl rounded-3xl border bg-white p-8 text-center shadow-sm"><p className="text-sm font-bold uppercase tracking-[.15em] text-[#c99b2f]">Account received</p><h1 className="mt-3 text-3xl font-black text-[#28114d]">Your student access is pending.</h1><p className="mt-4 leading-7 text-[#696477]">Coach Cocoa will review your request. Once activated, sign in again to enter the workspace.</p><form action="/auth/signout" method="post" className="mt-7"><button className="rounded-xl border border-[#6a35a6] px-4 py-3 font-bold text-[#53258c]">Sign out</button></form></section></main>;}
